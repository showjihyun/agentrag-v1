'use client';

/**
 * A2A Protocol Settings Page
 * 
 * Google A2A 프로토콜 설정 페이지
 */

import A2ASettings from '@/components/agent-builder/A2ASettings';

export default function A2APage() {
  return <A2ASettings />;
}
