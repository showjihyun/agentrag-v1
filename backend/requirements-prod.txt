# Production Dependencies

# Base requirements
-r requirements-base.txt

# ML requirements (optional - comment out if not using ML features)
-r requirements-ml.txt

# Monitoring and Observability
prometheus-client>=0.19.0,<1.0.0
opentelemetry-api>=1.21.0,<2.0.0
opentelemetry-sdk>=1.21.0,<2.0.0
opentelemetry-instrumentation-fastapi>=0.42b0,<1.0.0

# Error Tracking (optional - uncomment if using Sentry)
# sentry-sdk[fastapi]>=1.38.0,<2.0.0

# Performance
orjson>=3.9.10,<4.0.0  # Faster JSON serialization
ujson>=5.8.0,<6.0.0  # Alternative fast JSON

# Security
cryptography>=41.0.7,<42.0.0

# Production WSGI Server
gunicorn>=21.2.0,<22.0.0
uvicorn[standard]>=0.27.0,<0.28.0

# Health Checks
httpx>=0.27.0,<1.0.0
